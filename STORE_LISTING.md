# Chrome 网上应用店上架材料

面向 [Chrome 开发者控制台](https://chrome.google.com/webstore/devconsole)。下面的文案可直接复制粘贴。

## 上传包

上传 **`watch-together-store.zip`**（不是 crx）。重新打包命令：

```bash
cd ~/watch-together/extension && rm -f ../watch-together-store.zip \
  && zip -r -X ../watch-together-store.zip . -x '*.DS_Store' '*.svg'
```

## 基本信息

- **名称**：一起看 - 异地同步看视频
- **简短描述**（132 字以内）：
  和异地的 TA 一起看视频。实时同步 B站、腾讯视频、爱奇艺的播放、暂停、进度，边看边聊，就像坐在一起。

- **类别**：社交与通讯（主）/ 娱乐
- **语言**：简体中文

> 名称带上"异地同步看视频"是为了让人在商店里搜"一起看""同步看视频""异地看剧"时能搜到。

## 详细描述（复制粘贴）

```
异地也能一起看剧、看番、看电影。

无论是异地恋、分隔两地的家人，还是想和朋友同步追番的你，「一起看」让你们打开同一个视频，进度始终保持一致——一方按下播放、暂停或拖动进度条，另一方立刻同步跟上，再也不用喊"三、二、一，一起点"。

视频在各自的电脑上本地播放，画质不打折、不卡顿。插件只同步"播放控制信号"，不转发视频画面，所以不吃网速、不糊屏，比共享屏幕流畅得多。

【支持平台】
· 哔哩哔哩（bilibili）
· 腾讯视频
· 爱奇艺

【主要功能】
· 实时同步播放、暂停、拖动进度
· 网络波动、缓冲导致进度不一致时，自动对齐
· 屏幕角落的小圆标，随时发消息聊天，不遮挡观看画面
· 房间号 + 密码，只有你和 TA 进得来，私密安心
· 支持全屏观看

【怎么用】
1. 你和对方各自安装本插件
2. 打开同一个视频页面（同一集、同一清晰度）
3. 点击插件图标，填写约定好的房间号和密码，加入房间
4. 看到"对方已加入"，就可以一起看啦

【关于隐私】
插件只在你和对方之间传递"播放控制指令"和你主动发送的聊天文字，不收集、不上传任何个人信息，不记录浏览历史。房间通过房间号加密码隔离，只有你们两人收得到彼此的消息。

【小提示】
· 付费/会员内容需要各自用有会员的账号登录（插件不共享登录状态）
· 想让对方快速打开你正在看的视频？面板上点"喊 TA 来"，一键把当前页发给 TA
```

## 隐私实践（Privacy practices）——过审关键

控制台的「隐私」标签页要逐项填，如实按下面填即可：

### 单一用途说明（Single purpose）
```
同步异地两人观看同一在线视频的播放进度，并提供简单的文字聊天。
```

### 权限用途说明（每个权限都要写理由）

- **storage**：
  ```
  在本地保存用户填写的房间号、密码和连接偏好，以便下次自动重连。数据仅存于用户本机。
  ```

- **主机权限**（bilibili.com / v.qq.com / iqiyi.com）：
  ```
  插件需要在这三个视频网站的播放页面上运行，读取和控制页面中的视频播放器，以实现播放/暂停/进度的同步。仅在这些站点生效。
  ```

- **远程代码**：选「否，不使用远程代码」。（本插件所有 JS 都打包在扩展内，未从外部加载代码。）

### 数据用途声明（Data usage）

勾选与如实声明：
- 是否收集用户数据：**基本不收集**。插件传输的只有"播放控制指令"和"用户主动输入的聊天文字"，经中转服务器转发给同房间的另一方，**不落库、不分析、不用于任何其他目的**。
- 不收集：身份信息、健康信息、位置、浏览历史、个人通讯录等一律**不收集**。
- 不出售数据、不用于与核心功能无关的用途、不用于判断信用。

### 隐私政策链接（必填）

商店要求提供一个公开可访问的隐私政策网址。最省事的做法：
把仓库里的 `PRIVACY.md` 通过 GitHub 直接给出链接，例如：
```
https://github.com/Vizards8/watch-together/blob/main/PRIVACY.md
```

## 需要准备的图片素材

- **图标**：128×128，已有（extension/icon128.png）
- **截图**：1280×800 或 640×400，公开发布建议传 **3–5 张**，越能说明功能越好：
  1. 视频播放页 + 右下角小圆标（展示"不遮挡画面"）
  2. 展开的聊天面板（展示"边看边聊"）
  3. 插件弹窗填房间号/密码（展示"加入房间"）
  4.（可选）两个窗口并排、进度一致（展示"同步"效果）
- （可选但推荐）**小宣传图 440×280**：公开插件在商店分类页会用到，有它更显眼。

## 费用与流程

1. 首次注册开发者账号需一次性缴纳 5 美元
2. 上传 zip、填写以上信息、上传截图
3. 可见性选 **「公开」（Public）**，让所有人都能搜索和安装
4. 提交审核，通常 1–3 个工作日

## 公开发布的额外注意（审核更严，先自查）

- **描述与实际功能一致**：不要夸大（别写"支持所有视频网站"），只列真实支持的三家，否则会被打回。
- **截图真实**：截图必须是插件真实界面，不能用与功能无关的宣传画。至少 1 张，建议 3–5 张展示不同功能（同步、聊天面板、房间设置）。
- **主机权限最小化**：只申请了 bilibili/qq/iqiyi 三个域名，没有 `<all_urls>`，这点对公开审核很有利，保持现状。
- **不要出现商标/版权问题**：描述里提到平台名（B站、腾讯视频、爱奇艺）是说明兼容性，属合理使用；但**图标和截图里不要直接使用这些平台的 Logo**，避免商标纠纷。当前图标是自绘的播放键+爱心，没问题。
- **隐私政策必须可访问**：确保 PRIVACY.md 的 GitHub 链接能公开打开。
- **邮箱**：公开插件建议在开发者信息里填一个能收信的联系邮箱，用户和审核可能通过它联系你。

